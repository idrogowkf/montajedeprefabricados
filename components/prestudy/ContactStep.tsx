import type { PreStudyData } from "@/lib/prestudy";
import SubmissionSummary from "./SubmissionSummary";

export default function ContactStep({ data, update, errors }: { data: PreStudyData; update: (patch: Partial<PreStudyData>) => void; errors: Record<string,string> }) {
  const bind = (key: keyof PreStudyData) => ({value:String(data[key]),onChange:(event:React.ChangeEvent<HTMLInputElement|HTMLTextAreaElement>) => update({[key]:event.target.value})});
  return <div><h3>DATOS DE CONTACTO</h3><p className="sub">Ahora sí: el formulario ya contiene el contexto técnico de tu consulta.</p><div className="fields"><div className="field"><label htmlFor="name">Nombre</label><input id="name" {...bind("name")} aria-invalid={!!errors.name} />{errors.name && <span className="field-error">{errors.name}</span>}</div><div className="field"><label htmlFor="company">Empresa</label><input id="company" {...bind("company")} /></div><div className="field"><label htmlFor="phone">Teléfono</label><input id="phone" type="tel" {...bind("phone")} aria-invalid={!!errors.contact} /></div><div className="field"><label htmlFor="email">Email</label><input id="email" type="email" {...bind("email")} aria-invalid={!!errors.email || !!errors.contact} />{errors.email && <span className="field-error">{errors.email}</span>}</div>{errors.contact && <p className="field-error full" role="alert">{errors.contact}</p>}<div className="field full"><label htmlFor="message">Mensaje adicional</label><textarea id="message" {...bind("message")} /></div><div className="honeypot" aria-hidden="true"><label htmlFor="website">Sitio web</label><input id="website" tabIndex={-1} autoComplete="off" {...bind("website")} /></div></div><SubmissionSummary data={data} /></div>;
}
