import type { PreStudyData } from "@/lib/prestudy";

export default function PiecesStep({ data, update }: { data: PreStudyData; update: (patch: Partial<PreStudyData>) => void }) {
  const field = (key: keyof PreStudyData) => ({ value: String(data[key]), onChange: (event: React.ChangeEvent<HTMLInputElement | HTMLTextAreaElement>) => update({[key]:event.target.value}) });
  return <div><h3>DATOS DE LAS PIEZAS</h3><p className="sub">No necesitas tener todos los datos. Añade lo conocido y deja el resto vacío.</p><div className="fields"><div className="field"><label htmlFor="pieces">Número aproximado de piezas</label><input id="pieces" type="number" min="1" {...field("pieces")} /></div><div className="field"><label htmlFor="maxWeight">Peso máximo de una pieza (t)</label><input id="maxWeight" type="number" min="0" step="0.1" {...field("maxWeight")} /></div><div className="field"><label htmlFor="maxLength">Longitud máxima (m)</label><input id="maxLength" type="number" min="0" step="0.1" {...field("maxLength")} /></div><div className="field"><label htmlFor="placementHeight">Altura aproximada de colocación (m)</label><input id="placementHeight" type="number" min="0" step="0.1" {...field("placementHeight")} /></div><div className="field full"><label htmlFor="pieceNotes">Algo importante sobre las piezas</label><textarea id="pieceNotes" {...field("pieceNotes")} /></div></div></div>;
}
