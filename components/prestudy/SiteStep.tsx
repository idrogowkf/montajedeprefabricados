import type { PreStudyData } from "@/lib/prestudy";

export default function SiteStep({ data, update, error }: { data: PreStudyData; update: (patch: Partial<PreStudyData>) => void; error?: string }) {
  const bind = (key: keyof PreStudyData) => ({ value: String(data[key]), onChange: (event: React.ChangeEvent<HTMLInputElement | HTMLTextAreaElement | HTMLSelectElement>) => update({[key]:event.target.value}) });
  return <div><h3>EMPLAZAMIENTO Y ACCESOS</h3><p className="sub">La ubicación y los accesos cambian completamente una maniobra.</p><div className="fields"><div className="field"><label htmlFor="location">Provincia / ciudad</label><input id="location" {...bind("location")} aria-invalid={!!error} />{error && <span className="field-error" role="alert">{error}</span>}</div><div className="field"><label htmlFor="date">Fecha prevista de montaje</label><input id="date" type="date" {...bind("date")} /></div><div className="field full"><label htmlFor="address">Dirección o ubicación aproximada</label><input id="address" {...bind("address")} /></div><div className="field"><label htmlFor="storageArea">¿Hay zona de acopio?</label><select id="storageArea" {...bind("storageArea")}><option>No lo sé</option><option>Sí</option><option>No</option><option>Limitada</option></select></div><div className="field"><label htmlFor="access">Acceso de transporte pesado</label><select id="access" {...bind("access")}><option>No lo sé</option><option>Bueno</option><option>Limitado</option><option>Muy condicionado</option></select></div><div className="field full"><label htmlFor="constraints">Condicionantes conocidos</label><textarea id="constraints" {...bind("constraints")} placeholder="Líneas eléctricas, vía férrea, tráfico, pendientes, espacio reducido, horarios..." /></div></div></div>;
}
